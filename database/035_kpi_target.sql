-- 035_kpi_target.sql
-- เป้าหมายร้อยละของตัวชี้วัด (เช่น 90.00) ที่ HDC ให้มาพร้อมนิยาม
--
-- ทำไมต้องเก็บ: คำถามที่คนทำนโยบายถามบ่อยที่สุดคือ "ผ่านเกณฑ์ไหม" ซึ่งตอบไม่ได้
-- ถ้าไม่รู้เกณฑ์ · ต้นทางให้ค่านี้มาอยู่แล้วในฟิลด์ `target` ของ report-public/detail
-- แต่เดิมไม่ได้เก็บ ทำให้ AI ต้องรอให้คนบอกเกณฑ์ทุกครั้ง

ALTER TABLE csv_data_dict ADD COLUMN IF NOT EXISTS kpi_target VARCHAR(16);

COMMENT ON COLUMN csv_data_dict.kpi_target IS
  'เป้าหมายร้อยละของตัวชี้วัดจาก HDC เช่น "90.00" — ว่าง = ต้นทางไม่ได้กำหนด';
